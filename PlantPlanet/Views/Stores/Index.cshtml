@model IEnumerable<PlantPlanet.Models.Store>

@{
    ViewData["Title"] = "Index";
}
<link rel="stylesheet" href="~/css/filter.css" />

<div class="container">
    <div class="row">
        <div class="col-md-4">
            <a asp-action="Create" class="btn btn-outline-info">יצירת חנות חדשה</a>
        </div>
        <div class="col-md-4">

        </div>
        <div class="col-md-4">
            <button id="filterProducts" style="all:unset; padding-right:10%; display: flex; cursor: pointer">
                <i class="fa fa-search"></i>
                <h6 style="margin-right: 5px"> סינון חנויות</h6>
            </button>

            <div id="myModal" class="modal">
                <!-- Modal content -->
                <div class="modal-content" style="align-items:center">
                    <span class="close">&times;</span>
                    <form asp-action="Filter" asp-controller="Stores" style="margin-top: 4%">

                        <div class="container" style="text-align:center">
                            <h4>סינון חנויות</h4>
                            <br />
                            <div class="row">
                                <div class="col-md-6">
                                    <i>סינון לפי שם החנות</i>
                                </div>
                                <div class="col-md-6">
                                    <input name="NameQuery" class="search-input" type="search" placeholder="שם חנות" />
                                </div>
                            </div>
                            <br />
                            <div class="row">
                                <div class="col-md-6">
                                    <i>סינון לפי מספר סידורי</i>
                                </div>
                                <div class="col-md-5">
                                    <input name="idQuery" class="search-input" type="number" placeholder="מספר סידורי" />
                                </div>
                                <div class="col-md-1">
                                    <button class="search-container" style="border:none; background-color:transparent">
                                        <i class="fas fa-search" style="margin-right: 5px" aria-hidden="true"></i>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
            @*<form asp-action="Search" asp-controller="Stores" style="margin-right:3%">
            <input name="query" class="search-input" type="search" placeholder="חיפוש חנות" />
            <button class="search-container" style="border:none; background-color:transparent">
                <i class="fas fa-search" style="margin-right: 5px" aria-hidden="true"></i>
            </button>
        </form>*@
        </div>
    </div>
    <br />
    <div class="row" style="font-weight:bold">
        <div class="col-md-6">
            <h4>@Html.DisplayNameFor(model => model.StoreName)</h4>
        </div>
        <div class="col-md-6">
            <h4>אפשרויות ניהול</h4>
        </div>
    </div>
    @foreach (var item in Model)
    {
        <br />
        <div class="row">
            <div class="col-md-6">
                @Html.DisplayFor(modelItem => item.StoreName)
            </div>
            <div class="col-md-6">
                <a asp-action="Edit" asp-route-id="@item.StoreId">עריכה</a> |
                <a asp-action="Details" asp-route-id="@item.StoreId">פרטים</a> |
                <a asp-action="Delete" asp-route-id="@item.StoreId">מחיקה</a>
            </div>
        </div>
    }
</div>
<script src="~/js/filter.js"></script>